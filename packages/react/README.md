# @previewdock/react

PreviewDock 的 React 16.8–19 UI 接入层，提供 `ready`、`status` 和 `error` 回调及一致的卸载清理行为。

支持通过 `watermark` prop 为所有格式添加统一展示水印。

```bash
pnpm add react @previewdock/react @previewdock/preset-all
```

完整示例见[多框架接入文档](https://playground.yigeren.me/docs/frameworks)。

License: Apache-2.0
