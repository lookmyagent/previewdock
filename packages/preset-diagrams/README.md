# @previewdock/preset-diagrams

Official PreviewDock preset for BPMN, XMind, Visio, WMF, and EMF diagrams.

```ts
import { diagramsPack } from '@previewdock/preset-diagrams'
```

Legacy drawing conversion remains optional through `createDiagramsPack({ legacyOffice })`.
