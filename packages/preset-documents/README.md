# @previewdock/preset-documents

Official PreviewDock preset for Office, PDF, OpenDocument, OFD, RTF, EPUB, and email files.

```ts
import { documentsPack } from '@previewdock/preset-documents'
```

Legacy DOC/PPT and drawing conversion remains optional through `createDocumentsPack({ legacyOffice })`.
