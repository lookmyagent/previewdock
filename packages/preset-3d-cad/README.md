# @previewdock/preset-3d-cad

Official PreviewDock preset for interactive 3D models and engineering CAD files.

```ts
import { threeDCadPack } from '@previewdock/preset-3d-cad'
```

Engineering formats can receive WASM locations through `createThreeDCadPack({ model })`.
