# 格式支持与兼容性

“支持”表示可以在浏览器中呈现有用的只读内容，不代表与桌面专业软件像素级一致。下面的内容直接来自 PreviewDock 唯一格式目录，门户、Preset 和实验室使用同一份数据。

<FormatCatalog locale="zh" />

## 状态说明

- **标准预览**：提供对应类型的页面、表格、图像、媒体、图形或模型阅读能力；
- **结构 / 快速预览**：展示文件中的真实元数据、可读内容和字节结构，不冒充桌面专业软件的完整排版；
- 正式发布矩阵以真实样例、非空挂载和浏览器验收结果为准。

PreviewDock 不执行宏、嵌入脚本或其他活动内容。文件大小、压缩包展开量、媒体编码和模型资源应由宿主系统设置明确边界。

远程 ZIP/JAR 使用大文件模式时，文件服务器必须支持 HTTP Range。服务端不支持分段读取时，超过 100 MB 的文件不会完整下载到浏览器，而是显示明确的环境提示。
